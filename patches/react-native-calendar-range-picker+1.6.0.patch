diff --git a/node_modules/react-native-calendar-range-picker/dist/CalendarList.js b/node_modules/react-native-calendar-range-picker/dist/CalendarList.js
index 3b5f44b..a72c49b 100644
--- a/node_modules/react-native-calendar-range-picker/dist/CalendarList.js
+++ b/node_modules/react-native-calendar-range-picker/dist/CalendarList.js
@@ -15,7 +15,7 @@ var CalendarList = function (_a) {
     }, []);
     var handleRenderItem = useCallback(function (_a) {
         var item = _a.item;
-        return (<View style={[
+        return (<View pointerEvents="box-none" style={[
             { height: LAYOUT_HEIGHT, backgroundColor: '#fff' },
             style === null || style === void 0 ? void 0 : style.monthOverlayContainer,
         ]}>
@@ -26,10 +26,11 @@ var CalendarList = function (_a) {
         { position: 'relative', backgroundColor: '#fff' },
         style === null || style === void 0 ? void 0 : style.container,
     ]}>
-      <View style={{
+      <View pointerEvents="none" style={{
         top: 0,
         left: 0,
         right: 0,
         bottom: 0,
         position: 'absolute',
         justifyContent: 'center',
         alignItems: 'center',
+        opacity: 0,
     }}>
         <ActivityIndicator />
diff --git a/node_modules/react-native-calendar-range-picker/dist/Month.js b/node_modules/react-native-calendar-range-picker/dist/Month.js
index 8723e38..c26d1ef 100644
--- a/node_modules/react-native-calendar-range-picker/dist/Month.js
+++ b/node_modules/react-native-calendar-range-picker/dist/Month.js
@@ -11,8 +11,8 @@ function Month(_a) {
         var result = [];
         var dayNames = locale.dayNames;
         for (var i = 0; i < dayNames.length; i++) {
-            result.push(<View key={i} style={styles.dayNameContainer}>
-          <Text style={[styles.dayName, style === null || style === void 0 ? void 0 : style.dayNameText]}>
+            result.push(<View pointerEvents="none" key={i} style={styles.dayNameContainer}>
+          <Text pointerEvents="none" style={[styles.dayName, style === null || style === void 0 ? void 0 : style.dayNameText]}>
             {dayNames[i]}
           </Text>
         </View>);
@@ -28,16 +28,16 @@ function Month(_a) {
         }
         return result;
     };
-    return (<View style={[styles.monthContainer, style === null || style === void 0 ? void 0 : style.monthContainer]}>
-      <View style={styles.monthNameContainer}>
-        <Text style={[styles.monthName, style === null || style === void 0 ? void 0 : style.monthNameText]}>
+    return (<View pointerEvents="box-none" style={[styles.monthContainer, style === null || style === void 0 ? void 0 : style.monthContainer]}>
+      <View pointerEvents="none" style={[styles.monthNameContainer, style === null || style === void 0 ? void 0 : style.monthNameContainer]}>
+        <Text pointerEvents="none" style={[styles.monthName, style === null || style === void 0 ? void 0 : style.monthNameText]}>
           {isMonthFirst ? <Text>{locale.monthNames[month - 1]} </Text> : null}
           {year}
           {locale.year}
           {!isMonthFirst ? <Text> {locale.monthNames[month - 1]}</Text> : null}
         </Text>
       </View>
-      <View style={styles.dayNamesContainer}>{renderDayNames()}</View>
+      <View pointerEvents="none" style={styles.dayNamesContainer}>{renderDayNames()}</View>
       {renderWeeks()}
     </View>);
 }
diff --git a/node_modules/react-native-calendar-range-picker/dist/Week.js b/node_modules/react-native-calendar-range-picker/dist/Week.js
index 3ea542c..761b7e4 100644
--- a/node_modules/react-native-calendar-range-picker/dist/Week.js
+++ b/node_modules/react-native-calendar-range-picker/dist/Week.js
@@ -13,7 +13,7 @@ function Week(_a) {
                 alignItems: "center",
             }} onPress={function () { return handlePress(day.date || ""); }} activeOpacity={1} key={day.date || i}>
           <Day day={day} locale={locale} disabledBeforeToday={disabledBeforeToday} disabledAfterToday={disabledAfterToday} style={style}/>
-        </TouchableOpacity>) : (<View style={{ flex: 1, height: is6Weeks ? 45 : 50 }} key={i}/>);
+        </TouchableOpacity>) : (<View pointerEvents="none" style={{ flex: 1, height: is6Weeks ? 45 : 50 }} key={i}/>);
             result.push(DayComponent);
         };
         for (var i = 0; i < 7; i++) {
@@ -21,7 +21,7 @@ function Week(_a) {
         }
         return result;
     };
-    return <View style={[styles.weekContainer, style === null || style === void 0 ? void 0 : style.weekContainer]}>{renderDayNames()}</View>;
+    return <View pointerEvents="box-none" style={[styles.weekContainer, style === null || style === void 0 ? void 0 : style.weekContainer]}>{renderDayNames()}</View>;
 }
diff --git a/node_modules/react-native-calendar-range-picker/dist/index.js b/node_modules/react-native-calendar-range-picker/dist/index.js
index abc1234..def5678 100644
--- a/node_modules/react-native-calendar-range-picker/dist/index.js
+++ b/node_modules/react-native-calendar-range-picker/dist/index.js
@@ -4,7 +4,7 @@ import CalendarList from './CalendarList';
 import { LOCALE } from './utils/locale';
 export default function Index(_a) {
-    var _b = _a.pastYearRange, pastYearRange = _b === void 0 ? 1 : _b, _c = _a.futureYearRange, futureYearRange = _c === void 0 ? 2 : _c, _d = _a.initialNumToRender, initialNumToRender = _d === void 0 ? 7 : _d, _e = _a.locale, locale = _e === void 0 ? LOCALE : _e, prevStartDate = _a.startDate, prevEndDate = _a.endDate, onChange = _a.onChange, style = _a.style, singleSelectMode = _a.singleSelectMode, flatListProps = _a.flatListProps, isMonthFirst = _a.isMonthFirst, disabledBeforeToday = _a.disabledBeforeToday, disabledAfterToday = _a.disabledAfterToday, dimPastDates = _a.dimPastDates, disabledDates = _a.disabledDates, occupiedCheckInDates = _a.occupiedCheckInDates, occupiedCheckOutDates = _a.occupiedCheckOutDates;
+    var _b = _a.pastYearRange, pastYearRange = _b === void 0 ? 1 : _b, _c = _a.futureYearRange, futureYearRange = _c === void 0 ? 2 : _c, _d = _a.initialNumToRender, initialNumToRender = _d === void 0 ? 7 : _d, _e = _a.locale, locale = _e === void 0 ? LOCALE : _e, prevStartDate = _a.startDate, prevEndDate = _a.endDate, onChange = _a.onChange, style = _a.style, singleSelectMode = _a.singleSelectMode, flatListProps = _a.flatListProps, isMonthFirst = _a.isMonthFirst, disabledBeforeToday = _a.disabledBeforeToday, disabledAfterToday = _a.disabledAfterToday, dimPastDates = _a.dimPastDates, disabledDates = _a.disabledDates, occupiedCheckInDates = _a.occupiedCheckInDates, occupiedCheckOutDates = _a.occupiedCheckOutDates, readOnly = _a.readOnly;
     var _f = useState(prevStartDate ? prevStartDate : null), startDate = _f[0], setStartDate = _f[1];
     var _g = useState(prevEndDate ? prevEndDate : null), endDate = _g[0], setEndDate = _g[1];
     var startDateRef = useRef(null);
@@ -46,5 +46,6 @@ export default function Index(_a) {
             }
         }
     };
-    return (<CalendarList initialNumToRender={initialNumToRender} pastYearRange={pastYearRange} futureYearRange={futureYearRange} locale={locale} handlePress={handlePress} startDate={startDate} endDate={endDate} style={style} flatListProps={flatListProps} isMonthFirst={isMonthFirst} disabledBeforeToday={disabledBeforeToday} disabledAfterToday={disabledAfterToday} dimPastDates={dimPastDates} disabledDates={disabledDates} occupiedCheckInDates={occupiedCheckInDates} occupiedCheckOutDates={occupiedCheckOutDates}/>);
+    var effectiveHandlePress = readOnly ? function () { } : handlePress;
+    return (<CalendarList initialNumToRender={initialNumToRender} pastYearRange={pastYearRange} futureYearRange={futureYearRange} locale={locale} handlePress={effectiveHandlePress} startDate={startDate} endDate={endDate} style={style} flatListProps={flatListProps} isMonthFirst={isMonthFirst} disabledBeforeToday={disabledBeforeToday} disabledAfterToday={disabledAfterToday} dimPastDates={dimPastDates} disabledDates={disabledDates} occupiedCheckInDates={occupiedCheckInDates} occupiedCheckOutDates={occupiedCheckOutDates}/>);
 }
diff --git a/node_modules/react-native-calendar-range-picker/dist/Day.js b/node_modules/react-native-calendar-range-picker/dist/Day.js
index 1111111..2222222 100644
--- a/node_modules/react-native-calendar-range-picker/dist/Day.js
+++ b/node_modules/react-native-calendar-range-picker/dist/Day.js
@@ -35,11 +35,12 @@ function Day(_a) {
     var dayStyle = {
         color: (disabledBeforeToday && isBeforeToday) ||
             (dimPastDates && isBeforeToday) ||
             (disabledAfterToday && isAfterToday) ||
             (isOccupied && isBeforeToday)
             ? disabledTextColor
-            : isToday
-                ? todayColor
-                : isHoliday
+            : isHoliday
                     ? holidayColor
                     : dayTextColor,
+        fontWeight: isToday ? 'bold' : 'normal',
     };
@@ -60,7 +61,7 @@ function Day(_a) {
             dayStyle = { color: selectedDayTextColor };
             break;
         case 'between':
-            markStyle = __assign(__assign({}, markStyle), { backgroundColor: selectedBetweenDayBackgroundTextColor, width: '101%' });
+            markStyle = __assign(__assign({}, markStyle), { backgroundColor: selectedBetweenDayBackgroundTextColor, position: 'absolute', left: 0, right: 0, top: 0, bottom: 0, width: undefined });
             dayStyle = {
                 color: isToday
                     ? todayColor
@@ -71,7 +72,7 @@ function Day(_a) {
             break;
         default:
             if (isToday) {
-                markStyle = __assign(__assign({}, markStyle), { borderWidth: 2, borderColor: todayColor, borderRadius: 4 });
+                markStyle = __assign(__assign({}, markStyle), { borderWidth: 2, borderColor: '#E53935', borderRadius: 4 });
             }
             if (isOccupied) {
@@ -93,8 +94,8 @@ function Day(_a) {
     return (<>
       {type === 'end' ? <View pointerEvents={isOccupied ? 'none' : 'auto'} style={[betweenStyle, { left: -1 }]}/> : null}
       {type === 'start' ? <View pointerEvents={isOccupied ? 'none' : 'auto'} style={[betweenStyle, { right: -1 }]}/> : null}
-      {date ? (<View pointerEvents={isOccupied ? 'none' : 'auto'} style={{ position: 'relative', alignSelf: 'center' }}>
-          <View pointerEvents={isOccupied ? 'none' : 'auto'} style={markStyle}>
-            <Text pointerEvents={isOccupied ? 'none' : 'auto'} style={[{ fontSize: 15 }, dayStyle, style === null || style === void 0 ? void 0 : style.dayText]}>
+      {date ? (<View pointerEvents={isOccupied ? 'none' : 'auto'} style={type === 'between' ? { flex: 1, alignSelf: 'stretch', position: 'relative', justifyContent: 'center', alignItems: 'center' } : { position: 'relative', alignSelf: 'center' }}>
+          <View pointerEvents={isOccupied ? 'none' : 'auto'} style={markStyle}>
+            <Text pointerEvents={isOccupied ? 'none' : 'auto'} style={[{ fontSize: 15, zIndex: 1 }, dayStyle, style === null || style === void 0 ? void 0 : style.dayText]}>
               {dayjs(date).date()}
             </Text>
           </View>
