# ✅ QA-чеклисты и регресс

---

## Чеклист 1: Множественные отклонения

> Цель: убедиться, что история накапливается и UI обновляется без ручного refresh.

- [ ] Admin отклоняет объект #1 (reason="Причина 1") → история показывает 1 запись
- [ ] Agent отправляет повторно → Admin отклоняет #2 (reason="Причина 2") → история показывает 2 записи, #2 жирная
- [ ] Admin отклоняет #3 (reason="Причина 3") → 3 записи
- [ ] Admin отклоняет #4 (reason="Причина 3" — то же, что предыдущий) → 4 записи (одинаковая причина не мерджится)
- [ ] Admin отклоняет #5 → 5 записей, нумерация корректная (от 1 до 5 снизу вверх или от 5 до 1 сверху вниз — консистентно)
- [ ] Нет ошибок в консоли / alert'ов об ошибках схемы

---

## Чеклист 2: Reject через NotificationBell

- [ ] Admin открывает уведомление `property_submitted` → открывается правая review-панель (не modal в центре)
- [ ] Admin отклоняет → панель закрывается
- [ ] На `WebPropertiesScreen` у admin статус объекта обновляется без ручного reload
- [ ] История отклонений обновляется сразу после reject из bell
- [ ] Agent в другой сессии получает обновление через companyChannel
- [ ] Повторить для `edit_submitted` → review-панель (не DiffModal)

---

## Чеклист 3: Approve после reject

- [ ] Admin отклоняет объект
- [ ] Admin нажимает Edit → сохраняет без правок → объект становится `approved`
- [ ] `rejection_reason` очищен в БД (проверить SQL: `SELECT rejection_reason FROM properties WHERE id=...`)
- [ ] `property_rejection_history` сохраняет старые записи (история не удалена)
- [ ] ⏳ чип исчезает в левой карточке
- [ ] Блок "История отклонений" скрыт (статус approved)
- [ ] Agent получает уведомление `property_approved`

---

## Чеклист 4: Уведомления

- [ ] Agent создаёт объект → Admin получает `property_submitted`
- [ ] Admin одобряет → Agent получает `property_approved`
- [ ] Admin отклоняет → Agent получает `property_rejected`
- [ ] Agent исправляет и отправляет → Admin получает `edit_submitted`
- [ ] Admin одобряет через edit (auto-approve) → Agent получает `property_approved`

---

## Чеклист 5: Проверка истории в БД

```sql
-- Проверить накопление истории по конкретному объекту:
SELECT id, reason, rejection_type, rejected_by, created_at
FROM property_rejection_history
WHERE property_id = '<property_uuid>'
ORDER BY created_at DESC;

-- Ожидаемый результат: N строк (по числу отклонений), каждая уникальная по created_at
```

- [ ] После reject #1: 1 строка
- [ ] После reject #2: 2 строки
- [ ] После auto-approve: строки сохраняются (не удалены)
- [ ] Поле `rejected_by` заполнено (не null, если auth.uid доступен)

---

## Чеклист 6: Smoke-check после миграций

- [ ] `property_rejection_history` существует (см. `11_SQL_Sanity_Check_после_миграций.md`)
- [ ] RLS policies активны
- [ ] Agent не может INSERT в историю напрямую
- [ ] Owner может SELECT и INSERT
- [ ] Index существует: `idx_property_rejection_history_property_date`

---

## Чеклист 7: Регресс pending flow

- [ ] Agent создаёт объект → статус `pending` → ⏳ в карточке слева
- [ ] Agent видит "На проверке" badge в правой панели (без Edit / без Approve/Reject)
- [ ] Admin видит Approve/Reject block (не pending badge)
- [ ] После approve: ⏳ исчезает, объект обычный

---

## Чеклист 8: ReadOnly режим review-панели

- [ ] Поля не редактируются (FieldInput caretHidden, editable=false)
- [ ] FieldDropdown не открывается (disabled)
- [ ] FieldSelect не реагирует на нажатие (onPress=undefined)
- [ ] Курсор `not-allowed` на disabled полях
- [ ] Кнопки "Save" / "Добавить фото" не отображаются
- [ ] Кнопки Approve/Reject отображаются корректно

---

## Чеклист 9: Mobile UI parity для верхней зоны вкладок

- [ ] `База` и `Booking calendar` имеют одинаковый паттерн верхних 2 блоков (header + toolbar)
- [ ] Header в обеих вкладках: title center, bell right, одинаковый размер bell
- [ ] Бейджи уведомлений (unread/read): одинаковые размеры, позиция и визуальный стиль
- [ ] Toolbar в обеих вкладках: одинаковая высота search и action-кнопок
- [ ] Допустимы только различия: текст title, длина search, число action-иконок
- [ ] Контент (список/календарь/сетка) начинается ниже toolbar без наложений

---

## Факт прохождения этапов G и H (2026-03-28)

**Этап G — предрелизное тестирование. Итог: GO ✅**

| Группа тестов | Что проверено | Результат |
|---|---|---|
| G1 — Локации агента | Admin назначает локации → агент видит только их, без "чужих" | PASS |
| G2 — Ограничения агента | Кнопки "добавить/удалить локацию" скрыты для агента на всех экранах | PASS |
| G3 — Увольнение агента | Deactivate через кастомный ConfirmModal → агент теряет доступ, данные компании не удаляются | PASS |
| G4 — Team flow после F3 | Revoke invitation и Deactivate — через кастомные модалки, без window.confirm/alert | PASS |
| G5 — SQL sanity | Таблицы company_members, agent_location_access, property_rejection_history — без null-аномалий | PASS |

**Дополнительно проверено в рамках F1–F3:**
- Агент не видит локации других компаний (getCompanyLocations с company_id scoping).
- MemberPermissionsModal: права + локации в одном потоке, ошибки сохранения отображаются inline.
- MemberRow показывает 3 компактных тега прав (Listings / Edit / Bookings).
- Notification Bell: ошибки approve/reject — inline error banner с auto-dismiss 5 сек (без window.alert).
- WebAccountScreen.loadData: stale closure исправлен, локации обновляются автоматически при смене прав.

**Найденные minor-дефекты (исправлены до релиза):**
- Stale closure в `WebAccountScreen.loadData` → исправлено добавлением `initialUser` в deps.
- `window.alert` в `WebNotificationBell` → заменено на inline error banner.
- `setAgentLocationAccess`: delete без проверки ошибки → найдено в G, исправлено до релиза (добавлена явная проверка ошибки delete + fail-fast). Открытых хвостов нет.

**Этап H — деплой и мониторинг**

- Коммит `6e7e637` отправлен в `main` 2026-03-28.
- Деплой на Vercel запущен автоматически (GitHub → Vercel integration).
- Мониторинг 24–48 часов: наблюдение за ошибками в консоли и поведением approve/reject flow в продакшне.
- Статус: **в стадии мониторинга**.
