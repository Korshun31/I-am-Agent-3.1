# 📋 Краткий лог — 2026-04-11

## Фокус дня
Завершение TestFlight-загрузки билда 23, разбор багов в подмодуле "Создание события календаря", первый опыт работы в схеме "два окна Claude Code" (архитектор + исполнитель), формулирование стратегии specification-first development.

## Что сделано

| # | Задача | Действие | Результат |
|---|--------|---------|----------|
| 1 | TestFlight upload | Запущен `eas submit --platform ios --latest` для билда 22 (внутренний номер 23) | Билд успешно отправлен в App Store Connect, появился со статусом "Обработка" |
| 2 | Освоение Claude Code CLI | Korshun впервые работает через CLI вместо Cursor IDE; договорились о схеме "два окна" — Opus как архитектор/советник (read-only), второе окно как исполнитель | Новый workflow: думаем в одном окне, делаем в другом |
| 3 | Расследование бага календаря | Найдены два независимых бага в подмодуле "Создание события календаря": (1) ошибка при добавлении с напоминанием, (2) событие не появляется в UI без перезагрузки даже без напоминания | Установлены точные причины обоих |
| 4 | Баг #1 — устаревший trigger expo-notifications | В expo-notifications 0.32.16 (Expo SDK 54) формат trigger изменился. `scheduleReminder` использовал старый формат (просто Date), что приводило к ошибке. Заменён на новый: `{ type: SchedulableTriggerInputTypes.DATE, date: triggerDate, channelId? }` | Коммит `e35c7a9`. Создание события с напоминанием больше не падает |
| 5 | Баг #2 — stale closure в AgentCalendarScreen | Локальный state `customEvents` дублировал `calendarEvents` из `AppDataContext`. В `loadData` после `await refreshCalendarEvents()` вызывался `setCustomEvents(calendarEvents)` со старым значением из closure → новые события не отображались до перезагрузки. Удалён `customEvents` state и функция `loadEvents`, экран теперь работает с `calendarEvents` напрямую из контекста | Коммит `11d0a70`. Новые события появляются в UI мгновенно |
| 6 | Новые варианты напоминаний | Добавлены "за 1 неделю" (10080 мин) и "за 1 месяц" (43200 мин) в обоих модалках (mobile + web) и переводах (en/ru/th) | Коммит `2dd55df` |
| 7 | Черновик правил подмодуля "Calendar Events / Create" | Обсуждены и зафиксированы в диалоге правила CE-CR-1..7 (бизнес-логика), CE-CR-UX-1..5 (UX), CE-CR-RM-1..4 (напоминания), CE-CR-ST-1..3 (state management). Не сохранены отдельным файлом — будут включены в `docs/MODULE_RULES/calendar_events.md` при работе над модулем | Первая попытка формализации правил подмодуля |
| 8 | Карта модулей проекта | Составлена полная карта 11 модулей с подмодулями и оценкой критичности (🔴/🟡/🟢) | Зафиксирована в этом документе и в `docs/DEVELOPMENT_STRATEGY.md` |
| 9 | Стратегия specification-first | Сформулирован подход "сначала чертежи, потом стройка": правила модулей пишутся ДО рефакторинга и тестов. Метафора с архитектурными чертежами здания | Создан документ `docs/DEVELOPMENT_STRATEGY.md` |
| 10 | Известный UX gap | Зафиксировано: в Calendar Events нельзя перенести событие на другую дату ни при создании, ни при редактировании. Это не баг, это отсутствующая фича. Предлагаемое решение — кликабельный заголовок-датапикер | Будет адресовано отдельной задачей при работе над модулем |
| 11 | Изучение концепций | Обсуждены: технический долг, orphan/dormant модули, рефакторинг (виды и масштабы), почему "большое переписывание с нуля" опасно. Korshun теперь понимает связь правил, тестов, технического долга и рефакторинга как единой триады | Концептуальная подготовка к работе по новой методологии |

## Известные проблемы после сегодняшнего дня
- TestFlight билд 23 НЕ содержит фиксов из коммитов `e35c7a9`, `11d0a70`, `2dd55df`. Нужен новый билд (24+) перед следующей отправкой тестировщикам.
- 3 коммита локально на main, не запушены. Решение о пуше отложено.

## Главный вывод
Сегодня проект перешёл от "итеративной разработки по запросам" к **методологии specification-first**. Это фундаментальный сдвиг — теперь любая работа над модулем начинается с правил, а не с кода. Сегодняшний учебный кейс с Calendar Events показал на практике как два совершенно разных типа багов (устаревший внешний API + классическая ошибка работы с React state) могли бы быть пойманы тестами, если бы существовали правила и тесты для подмодуля.
