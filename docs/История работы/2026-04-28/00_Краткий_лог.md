# 📋 Краткий лог — 2026-04-28

## Фокус дня
Закрыта большая фича **TD-086 «Передача брони агенту»** — от схемы базы и триггеров до пикера в формах на веб и мобильной версиях, плюс уведомления по теме (передача брони, изменение брони, назначение нового дома агенту). Семь функциональных тестов end-to-end в sandbox прошли, кросс-модульный аудит — без блокеров.

## Бизнес-контекст
До TD-086 у брони было только одно понятие — «создатель», и оно же выступало «ответственным». Это работало пока агент сам создавал свои брони, но мешало реальной практике: админ часто заводит бронь за агента (агент в дороге, на стойке заселения, или просто занят). После закрытия TD-086 у брони теперь два разных понятия:

- **Создатель** (`bookings.user_id`) — кто физически нажал «Создать».
- **Ответственный** (`bookings.booking_agent_id`) — кто за неё отвечает. NULL = «на Компанию» (админ), UUID = конкретный агент.

Админ через пикер в форме брони может оставить «Компания» или назначить агента дома. Агент пикер не видит и автоматически становится ответственным за свои создаваемые брони. После закрытия TD-099 (следующая задача) агент полноценно увидит и контактные данные клиентов своих переданных броней.

## Согласование сценария с владельцем

В первой половине дня было серьёзное обсуждение сценария — несколько раундов вопросов и уточнений на простом языке, чтобы убедиться что понимание совпадает. Финальная согласованная картина:

- Админ всегда видит и редактирует всё в своей компании.
- Агент видит брони только на тех домах где он ответственный за дом. На своих бронях (где он ответственный за саму бронь) — детали и редактирование, если включён `can_book`. На чужих (компанейских и от собственника) — только полоска занятости с именем компании или «Клиент собственника», полоска не кликабельна.
- Агент создаёт брони только на своих домах при включённой галочке «может работать с бронированиями».
- Уведомления: новому ответственному при передаче брони, текущему ответственному при правке полей админом, новому ответственному агенту дома при смене ответственного у property. Прежним при отзыве — НЕТ. Бронь от собственника по обычным правилам ответственности.
- Каскад: при смене ответственного дома все идущие и будущие брони (`check_out >= CURRENT_DATE`) → «Компания». Прошлые не трогаем.
- На дашборде у админа в счётчике — все брони компании, кроме броней от собственника. У агента — только те где он ответственный, и тоже без броней от собственника.

Записан в память финальный план фичи: `memory/project_booking_owner_transfer_plan.md`.

## Этапы реализации

### 1. Миграция БД (коммит `ebc0709`)

Файл: `supabase/migrations/20260428000000_booking_assignment_integrity.sql`. Накатано в sandbox через SQL Editor после ручной чистки 65 несоответствующих броней (где `booking_agent_id` = создателя, не равного `responsible_agent_id` дома).

Содержимое:
- Триггер целостности `trg_enforce_booking_agent_matches_property` на bookings — `booking_agent_id` либо NULL либо равен `properties.responsible_agent_id` соответствующего дома; иначе `RAISE EXCEPTION 'BOOKING_AGENT_MISMATCH'` (errcode 23514).
- Триггер каскада `trg_cascade_property_responsible` на properties — при смене `responsible_agent_id` дома все брони этого дома где `check_out >= CURRENT_DATE` обнуляют `booking_agent_id` в NULL.
- Patch RLS для `bookings: agent insert own` и `bookings: agent update own` — агент может писать только `booking_agent_id = self` или NULL; админ (company_owner) — любое валидное.
- В `notifications` добавлена колонка `booking_id UUID REFERENCES bookings(id) ON DELETE CASCADE` + индекс.
- RPC `create_notification` расширена параметром `p_booking_id UUID DEFAULT NULL` (обратно совместимо).

В файле миграции прописана инструкция в шапке: какие SELECT/UPDATE команды запустить ДО накатки в prod чтобы не сломать существующие брони.

### 2. JS-слой и UI (коммит `2f018b3`)

10 файлов, +359/−26 строк:

- `src/services/notificationsService.js` — `sendNotification` принимает опциональный `bookingId`, пробрасывает в RPC.
- `src/services/bookingsService.js` — добавлены хелперы (`isCompanyOwner`, `formatBookingNotificationBody`, `loadBookingNotificationContext`); `mapBooking` отдаёт `responsibleAgentId` и `isCompanyBooking`; `createBooking` различает роль (admin vs agent) и шлёт `booking_assigned` если ответственный не сам отправитель; `updateBooking` поддерживает смену ответственного админом, авто-сбрасывает при смене propertyId, шлёт `booking_assigned` (case A) или `booking_updated` (case B); `deleteBooking` симметрично — фильтр по `booking_agent_id` для агента.
- `src/services/propertiesService.js` — `updatePropertyResponsible` шлёт `property_assigned` новому агенту дома.
- `src/services/companyService.js` — `getActiveTeamMembers` фильтрует `status='active'` (закрывает B21 — пикер ответственного дома больше не показывает уволенных).
- `src/web/screens/WebDashboardScreen.js`, `src/web/screens/WebBookingsScreen.js`, `src/screens/BookingDetailScreen.js` — 9 потребителей `b.agentId` переключены на `b.responsibleAgentId`. Это закрывает B1.
- `src/web/components/WebBookingEditPanel.js` — новая секция «Ответственный за бронь» между блоками «Объект» и «Клиент», видна только админу, две кнопки (имя компании / имя агента дома), грузит активных членов команды, авто-сбрасывает поле при смене дома (фикс блокера от tester-агента), удалён дубликат ключа `reminderDays` в payload (древний баг до TD-086).
- `src/components/AddBookingModal.js` — аналогичный пикер на мобильной форме в Step 3 после блока pets.
- `src/i18n/translations.js` — ключ `bkResponsible` в en/th/ru.

### 3. Доводка `property_assigned` (коммит `22ae879`)

Тестирование вживую показало: уведомление новому ответственному агенту дома не приходит. Причина — обычная смена ответственного через карточку дома идёт через `updateProperty` (поле есть в whitelist `ALLOWED_CLIENT_FIELDS` после TD-051), а уведомление было повешено только на `updatePropertyResponsible` (которая используется для каскада на parent резорт/кондо).

Доработка `updateProperty` в `propertiesService.js`: если `updates` содержит `responsible_agent_id`, делается snapshot предыдущего значения, после успешного UPDATE — отправляется `property_assigned` новому агенту (если новый != null, != sender, != предыдущего). Best-effort через try/catch.

## Тестирование (live на crm.iamagent.app)

Семь сценариев:

1. **Админ создаёт бронь без выбора ответственного** → агент дома (Borat) видит «Co LTD Korshun 31» на полоске, не кликабельна.
2. **Админ создаёт бронь и сразу передаёт агенту** → Borat получает уведомление, видит как свою, кликабельна, можно редактировать. Имя клиента не отображается — это известное ограничение, закрывается следующим TD-099.
3. **Агент создаёт свою бронь** → пикер не показывается, бронь сохраняется на агента, попадает в «Мои брони» на дашборде, уведомлений никому.
4. **Админ редактирует чужую бронь агента** → агент получает уведомление об изменении, поле обновляется.
5. **Передача от одного агента к другому** — пропущен (у дома только один ответственный, такой переход возможен только через смену ответственного дома, что покрывается тестом 6).
6. **Смена ответственного у дома** → у старого агента дом исчезает, у нового — появляется. Брони уходят в «Компания» (каскад). Новый агент получает уведомление о назначении дома.
7. **Бронь от собственника** → полоска с «Клиент собственника», не кликабельна, уведомлений нет.

## Кросс-модульный аудит перед TD-099

Tester-агент проверил совместимость TD-086 со всеми правками сегодняшнего дня (TD-018, TD-101, TD-051, TD-085, TD-032, регрессия TD-011) и предыдущими модулями. Один false positive (двойное уведомление при сохранении parent-resort) — отвергнут после личной проверки кода (поле `responsible_agent_id` не попадает в payload `updateProperty` для parent/child). Реальных конфликтов нет, ветка готова к старту TD-099.

## Связанные задачи закрытые попутно

- **B1** — `mapBooking` читал `user_id` вместо `booking_agent_id`. Закрыто переключением 9 потребителей на новое поле `responsibleAgentId`.
- **B21** — пикер ответственного дома показывал деактивированных агентов. Закрыто фильтром `status='active'` в `getActiveTeamMembers`.

## Что НЕ входило в TD-086 и осталось на потом

- **TD-099** — RLS contacts по `booking_agent_id` (агент должен видеть контакт-клиента переданной ему брони). Следующая задача.
- **TD-107** — убрать JS-костыль фильтрации контактов в `WebContactsScreen` после TD-099.
- **Локализация уведомлений по языку получателя** — сейчас все уведомления на английском. Отдельный TD.
- **TD-087** — UI галочки `can_delete_booking` в карточке агента. Существующий TD.

## Уроки сессии

Предыдущие правила в памяти (`feedback_audit_before_constraint.md`, `feedback_compat_check_after_edit.md`) полностью сработали — Plan/Tester находили реальные блокеры до коммита: дублирующийся ключ `reminderDays`, отсутствующий сброс ответственного при смене дома, фильтр по `user_id` вместо `booking_agent_id` в редактировании агентом. Ошибся только в одном месте (повесил уведомление на `updatePropertyResponsible` вместо `updateProperty`) — поймали при live-тестировании, дополнительный фикс в коммите `22ae879`.

Подтвердилось правило `feedback_human_only_strict.md`: тестирование, обсуждение и формулировки — только на человеческом языке. Когда в одном моменте перешёл на жаргон («побочный канал», «whitelist», «propertyId»), владелец отметил недопонимание и попросил переформулировать. Перешёл обратно на человеческий — диалог сразу пошёл быстрее.

## TD-099 — RLS contacts через bookings (коммит `2dafc75`)

После TD-086 у агента осталась видимая дыра: при открытии переданной ему брони имя клиента, телефон и паспорт не показывались — политика доступа к таблице `contacts` пускала агента только к его «собственным» контактам и к owner-ам объектов где он ответственный, но не к контактам клиентов из чужих по созданию броней.

Исправление — миграция `supabase/migrations/20260428000001_contacts_agent_read_booking_clients.sql`: новая политика `contacts: agent reads booking clients` на SELECT, которая возвращает строки `contacts` чьи `id` есть в `bookings.contact_id` для броней где `booking_agent_id = auth.uid()`. Накатано в sandbox, проверено через `pg_policy`. JS-костыль фильтрации в `WebContactsScreen` оставлен на месте — его уберёт следующий TD-107 (отдельным проходом, чтоб не перемешивать «политика появилась» с «убрали обходной путь»).

При live-тестировании TD-099 всплыл соседний баг (коммит `a9c1d13`, потом `2dafc75` его закрепил): на странице Бронирования закэширован список объектов с полем `responsible_agent_id`, и от этого зависит показ пикера «Ответственный» в форме брони. `onPropertiesChanged` в `WebMainScreen` поднимал только ключ обновления для вкладки Объекты, а Бронирования продолжали читать старый список. После переназначения дома админ заходил создавать бронь — пикер исчезал. Теперь оба ключа поднимаются синхронно.

## Кросс-экранные дыры синхронизации (коммит `aa0467d`)

После случая с пикером (выше) попросил tester-агента провести более широкий аудит — что ещё не обновляется автоматически между вкладками. Нашёл три дыры:

- **Дашборд** подписывался только на `refreshKey.calendar_events`. Счётчики «Мои брони», «Активные», «Будущие» и список ближайших заездов оставались устаревшими после любой правки брони или объекта в других вкладках. `WebDashboardScreen` теперь получает композитный ключ `bookings + properties + calendar_events`.
- **Контакты** не обновлялись после изменения броней. После TD-099 это особенно болезненно: агенту через RLS открылся доступ к контактам клиентов его новых броней, но без отдельного события «контакты» страница Контакты не перечитывала список. Колбэк `bookings` в `companyChannel` теперь дополнительно поднимает ключ контактов.
- **Команда** не обновлялась у админа после того как новый сотрудник принял приглашение. `joinCompanyViaInvitation` завершалась тихо. Добавил `broadcastOneShot(companyId, 'team')` сразу после успешной RPC — список команды теперь обновляется во всех открытых сессиях подписчиков канала компании.

После этого жёстко записал в память правило `feedback_test_all_user_flows.md`: при QA-ревью узких промтов «проверь файлы X/Y/Z на регрессии» — недостаточно. Тестер обязан пройти все пользовательские сценарии и особенно проверить кросс-экранную синхронизацию.

## Унифицированная карточка «Бронирования» на дашборде (коммит `eeb09d5`)

Раньше карточка отличалась для админа и агента: админ видел `stats.occupied` как главную цифру и подписи «Мои клиенты / Прочие брони»; агент — `myAgencyActive` плюс другие подсчёты. Заказ владельца — обе роли должны видеть одну форму карточки: главная цифра = активные + будущие брони, под ней две подписи «Активные: N» и «Будущие: N». Бизнес-логика фильтрации (админ видит брони компании, агент — свои) уже была заложена выше по слою в `bookingsForStats` и не менялась — только верстка и подписи.

## Apple-style редизайн шапки боковой панели брони (коммит `bff7c6c` + правки в working tree)

Сначала подключил designer-агента для оценки правой панели «Детали брони». Первый раунд правок ушёл в `bff7c6c`: добавил отдельную секцию «Ответственный» (видна только админу, между Клиент и Стоимость, читает имя из `teamMembers` или название компании при NULL); кнопка «Подтверждение» уменьшена до иконки 📄 с подсказкой (раньше две большие кнопки переполняли панель 360px и перекрывали имя дома); код и город получили `numberOfLines + ellipsizeMode + flexShrink` чтобы цветной кружок типа объекта не наезжал на перенесённый текст; `infoValue` получил `minWidth: 90` чтобы суммы не переносились под длинными русскими подписями. Попутно — у плавающей кнопки FlightTracker появился `accessibilityLabel` и тултип «Flight tracker», раньше при наведении показывалось служебное `View`.

Второй раунд (коммит `62d7138` — вместе с фильтром «Ответственный») — Apple-style полная перевёрстка верхней зоны панели: верхняя строка с действиями слева (📄 «печать», ✏️ «редактировать») и крестиком закрытия справа через `space-between`; ниже зона идентичности — статус-бэйдж и имя дома в одной строке, под ним строка с цветным кружком типа и кодом+городом. Кнопка «Изменить» убрана как полнотекстовая — заменена иконкой ✏️ в том же стиле что и 📄. Дата заезда и дата выезда выровнены по высоте через `alignItems: 'stretch'`, разделитель с количеством ночей оказывается строго посередине. Понадобилось подгрузить `Platform` в импорты `WebBookingsScreen.js` (отвалилось при первой версии — `Platform is not defined`).

## Фильтр «Ответственный» в шапке Бронирования (коммит `62d7138`)

В верхней панели страницы «Бронирования» (рядом с фильтрами «Все / Мои клиенты / Район / Спальни / Питомцы / Дальние даты») добавлен новый выпадающий фильтр «Ответственный». Виден только админу. Single-select. Опции — активные члены команды у которых хотя бы один назначенный дом (`responsible_agent_id`), плюс при наличии — пункт «Без ответственного» (для домов «на компании»). Выбор фильтрует и Гант-календарь под шапкой (через список `visibleProps`), и режим списка (через те же `visibleProps`). Сбрасывается общей кнопкой сброса фильтров.

В память записан TODO для мобильной версии: `project_mobile_responsible_filter.md` — повторить аналогичный фильтр в мобильном приложении когда дойдём до его полировки.

## Коммиты дня
- `ebc0709` feat(db): booking_agent_id integrity triggers + notifications.booking_id
- `2f018b3` feat(TD-086): booking owner transfer — JS service + UI pickers
- `22ae879` fix(TD-086): notify new responsible agent on plain property update
- `0220b49` docs(TD-086): day log 2026-04-28 + progress plan update
- `2dafc75` fix(TD-099 + TD-086): contacts RLS + bookings tab refresh on property change
- `a9c1d13` fix: bookings tab refreshes properties cache on property change
- `aa0467d` fix: cross-screen state sync — dashboard, contacts, team
- `eeb09d5` feat: unified bookings card on dashboard — Active + Upcoming
- `bff7c6c` feat: booking detail panel — responsible section + cleaner header
- `62d7138` feat: bookings page — apple-style detail header + responsible-agent filter

## Файлы дня
- Миграции: `supabase/migrations/20260428000000_booking_assignment_integrity.sql`, `supabase/migrations/20260428000001_contacts_agent_read_booking_clients.sql`
- Изменены: `src/services/notificationsService.js`, `src/services/bookingsService.js`, `src/services/propertiesService.js`, `src/services/companyService.js`, `src/web/WebMainScreen.js`, `src/web/components/WebBookingEditPanel.js`, `src/web/components/WebFlightTracker.js`, `src/components/AddBookingModal.js`, `src/web/screens/WebDashboardScreen.js`, `src/web/screens/WebBookingsScreen.js`, `src/screens/BookingDetailScreen.js`, `src/i18n/translations.js`

## Уроки сессии (дополнение)

Узкие промты «проверь файлы X/Y/Z на регрессии» для tester-агента — недопустимы как единственный сценарий ревью. После случая с устаревшим кэшем объектов на вкладке Бронирования (пикер исчезал после переназначения дома) и тремя соседними дырами кросс-экранной синхронизации (Дашборд, Контакты, Команда) владелец справедливо указал: «надо все проверять, все возможные вариации которые может произвести пользователь». Записано в память жёстким правилом `feedback_test_all_user_flows.md`. При любом QA-ревью теперь широкий чеклист по всем пользовательским сценариям + отдельный блок «синхронизация state между экранами».

В части UI/UX подтвердилось ценное правило — когда дизайн «не нравится» владельцу, не пытаться угадать самостоятельно, а сразу подключать designer-агента и работать итерациями. Apple-style верхняя зона боковой панели брони — результат двух раундов с designer'ом и трёх раундов уточнений по мелочам от владельца («крестик отдельно справа», «кнопки слева», «дата заезда — выезда — стрелка по центру»).

## Следующая задача

Продолжение Фазы 2 «Контакты + RLS» по `docs/PROGRESS_PLAN.md`: TD-107 (убрать JS-костыль фильтрации контактов в `WebContactsScreen` после TD-099), затем TD-105/TD-106 (предупреждения при удалении owner и client), TD-103/TD-104/TD-100 (фото и документы). Хвост Фазы 1 — TD-033 (PKCE для OAuth в мобильном приложении) — отложен до отдельной мобильной сессии вместе с другими накопившимися мобильными хвостами.
