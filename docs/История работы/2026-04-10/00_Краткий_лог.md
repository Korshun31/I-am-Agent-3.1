# 📋 Краткий лог — 2026-04-10

## Фокус дня
Улучшение поиска и UX в Base и Calendar вкладках: поиск по собственнику, кнопка папки, скролл календаря.

## Что сделано

| # | Задача | Действие | Результат |
|---|--------|---------|----------|
| 1 | Поиск по собственнику — без фильтра | В RealEstateScreen.js топ-уровень (no-filter mode): поиск по owner_id/owner_id_2 через contacts | Поиск работает по имени собственника в обоих режимах |
| 2 | Кнопка папки при поиске | Условие `{hasActiveFilter && (` заменено на `{(hasActiveFilter \|\| searchQuery.length > 0) && (` | Кнопка раскрыть/свернуть появляется и при активном поиске |
| 3 | Сворачивание при сбросе поиска | useEffect на hasActiveFilter дополнен: сворачивать только если и фильтр неактивен, и поиск пустой | Объекты не сворачиваются пока введён поиск |
| 4 | Скролл календаря — сохранение позиции | Добавлен `hasScrolledOnceRef`: при первом открытии → initialScrollX, при повторном → сохранённая позиция | При переключении вкладок позиция скролла восстанавливается |
| 5 | Центрирование текущего месяца | `initialScrollX`: `idx * MONTH_WIDTH` → `idx * MONTH_WIDTH - SCREEN_WIDTH / 2 + MONTH_WIDTH / 2` | Текущий месяц открывается по центру экрана |
| 6 | Инициализация timelineScrollXRef | После useMemo initialScrollX добавлен useEffect: записывает initialScrollX в ref при первом запуске | При возврате из карточки объекта скролл на правильной позиции |

## Итог дня
Завершена серия улучшений UX в двух ключевых вкладках. Поиск стал умнее (находит по собственнику), кнопка раскрытия работает корректно, календарь открывается на текущем месяце по центру и запоминает позицию при навигации.

## В процессе
Ничего незавершённого.
