# 📋 Краткий лог — 2026-04-10

## Фокус дня
Улучшение поиска и UX в Base и Calendar вкладках: поиск по собственнику, кнопка папки, скролл календаря, ширина левой колонки.

## Что сделано

| # | Задача | Действие | Результат |
|---|--------|---------|----------|
| 1 | Поиск по собственнику — без фильтра (Base) | В RealEstateScreen.js topLevel.filter: поиск по owner_id/owner_id_2 через contacts | Поиск по имени работает в обоих режимах (с фильтром и без) |
| 2 | Кнопка папки при поиске (Base) | Условие `{hasActiveFilter && (` → `{(hasActiveFilter \|\| searchQuery.length > 0) && (` | Кнопка раскрыть/свернуть видна и при активном поиске |
| 3 | Не сворачивать при активном поиске (Base) | useEffect на hasActiveFilter: добавлено `&& searchQuery.length === 0` | Объекты не сворачиваются пока введён поиск |
| 4 | Поиск без фильтра в Calendar | filterFn возвращает false без фильтра; в listToShow — отдельный путь: все объекты без filterFn | При поиске без фильтра показываются все объекты, отфильтрованные только по тексту |
| 5 | Расширенный поиск в Calendar | Добавлены parentCode, ownerName, owner2Name через contacts | Поиск находит по коду курорта и имени собственника |
| 6 | Крестик в поиске Calendar | Добавлена кнопка-крестик в TextInput поисковой строки | Одно нажатие очищает поиск |
| 7 | Dismiss keyboard в Calendar | Пустое состояние обёрнуто в TouchableWithoutFeedback + Keyboard.dismiss | Клавиатура скрывается при нажатии на пустую область |
| 8 | Сохранение позиции скролла при смене вкладки | Добавлен `hasScrolledOnceRef`: первое появление → initialScrollX, повторное → timelineScrollXRef | Позиция скролла сохраняется при переключении вкладок |
| 9 | Fallback при нулевом скролле | В else-ветке: `timelineScrollXRef.current > 0 ? ... : initialScrollX` | Корректный скролл когда пользователь не скроллил |
| 10 | Инициализация timelineScrollXRef | useEffect после initialScrollX useMemo: записывает значение в ref при первом запуске | Возврат из карточки объекта скроллит на правильную позицию |
| 11 | Центрирование текущего месяца | `initialScrollX`: `idx * MONTH_WIDTH` → `idx * MONTH_WIDTH - SCREEN_WIDTH / 2 + MONTH_WIDTH / 2` | Текущий месяц открывается по центру экрана |
| 12 | Восстановление скролла после карточки объекта | useEffect на `selectedPropertyForDetail`: при null → scrollTo targetX | Скролл восстанавливается при возврате из PropertyDetailScreen |
| 13 | Восстановление скролла при появлении объектов | useEffect на `listToShow.length > 0`: scrollTo с timeout 100ms | Скролл восстанавливается когда объекты появляются через поиск/фильтр |
| 14 | Ширина левой колонки — MIN | MIN_COL_WIDTH: 60 → 100 | Минимальная ширина колонки достаточна для коротких кодов |
| 15 | Ширина левой колонки — MAX убран | Убран Math.min(MAX_COL_WIDTH, w) из leftColWidth | Колонка растягивается по содержимому без ограничения сверху |
| 16 | Отступы в левой колонке | COL_PADDING: 13 → 20; paddingHorizontal в propertyRow: 6 → 10 | Расчёт ширины и реальные отступы согласованы |
| 17 | CHAR_WIDTH откалиброван | CHAR_WIDTH: 8 → 7 (соответствует fontSize 12) | Колонка точнее подгоняется под реальную ширину текста |
| 18 | Откат MIN_COL_WIDTH = MONTH_WIDTH | git revert + повторная правка с явным числом 100 | Итоговое значение MIN_COL_WIDTH = 100 (не через константу) |
| 19 | Удаление debug-логов замера производительности | Удалены 5 console.log с Date.now() из MainNavigator.js, BookingCalendarScreen.js, RealEstateScreen.js | Логи не засоряют консоль; логика рендера и видимости не тронута |
| 20 | Google OAuth — настройка Google Cloud | Создан проект "I am Agent" в Google Cloud, настроен OAuth consent screen, создан Web client, добавлены redirect URIs | Google OAuth client готов |
| 21 | Google OAuth — настройка Supabase | Включён Google provider в Supabase, добавлены Site URL и Redirect URLs (Vercel + localhost) | Supabase принимает Google OAuth |
| 22 | Google OAuth — реализация в коде | Установлены expo-auth-session + expo-web-browser, добавлена signInWithGoogle() в authService, подключена кнопка в Login.js, убрана кнопка Apple | Вход через Google работает на вебе |
| 23 | supabase.js — detectSessionInUrl | detectSessionInUrl: false → Platform.OS === 'web' | Токен из URL подхватывается на вебе после OAuth redirect |
| 24 | app.json — scheme | Добавлен scheme: "iamagent" | Deep link для мобильного OAuth готов |
| 25 | Переименование таблицы agents → users_profile | ALTER TABLE agents RENAME TO users_profile в Supabase; обновлены authService.js (6 мест), propertiesService.js (3 места), get_company_team_function.sql, CURSOR_RULES.md | Таблица переименована, FK обновлены автоматически |
| 26 | Пересоздание функции get_company_team | Функция пересоздана в Supabase SQL Editor с JOIN users_profile вместо agents | Команда снова отображается корректно |
| 27 | RLS политика agents_self → users_profile_self | ALTER POLICY agents_self ON users_profile RENAME TO users_profile_self | Название политики соответствует новому имени таблицы |
| 28 | Восстановлен план korshun для korshun31@list.ru | UPDATE users_profile SET plan = 'korshun' | План восстановлен после переименования таблицы |
| 29 | Триггер auto-create profile и company | Создан handle_new_user() триггер на auth.users — автоматически создаёт users_profile, companies, company_members при регистрации любым способом | Новые пользователи получают профиль и компанию автоматически |
| 30 | signUp — upsert вместо insert | .insert() → .upsert({onConflict: 'id'}) чтобы избежать конфликта с триггером | Регистрация через email работает без ошибок |
| 31 | Удалены тестовые пользователи | Удалены adefender123@gmail.com и 31samui31@gmail.com из auth.users и users_profile | БД очищена от тестовых данных |
| 32 | Inline ошибка на экране логина | Заменены Alert.alert на inline красную строку под кнопкой; обновлён текст wrongPassword на всех языках | Пользователь видит ошибку прямо на экране без браузерного алерта |
| 33 | Inline ошибки на экране регистрации | Заменены Alert.alert на inline красную строку в Registration.js | Ошибки показываются прямо на экране |
| 34 | Убрано слово "агент" из заголовка регистрации | createAccount: 'Create Agent Account' → 'Create Account' на EN/TH/RU | Терминология соответствует правилам |
| 35 | Keyboard navigation на Login и Registration | Enter переключает поля и отправляет форму; Tab пропускает иконки глаза | Удобная навигация с клавиатуры |
| 36 | Исправлен баг с тарифом korshun | user?.role заменён на user?.plan в WebAccountScreen.js и MyDetailsEditModal.js; 'admin' заменён на 'korshun' в проверках доступа | Тариф korshun работает корректно — уведомления, approve/reject, лимиты фото |

## Итог дня
Масштабный день: Google и Facebook OAuth реализованы (кнопки временно скрыты до решения вопроса безопасности), таблица agents переименована в users_profile, исправлено отображение тарифов, реализован триггер автоматического создания профиля и компании при регистрации, регистрация через email работает полностью.

## В процессе
Ничего незавершённого.
