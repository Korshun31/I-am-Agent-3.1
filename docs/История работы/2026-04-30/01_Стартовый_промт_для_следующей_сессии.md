# 🚀 Стартовый промт для следующей сессии

> Скопируй этот текст целиком и отправь себе в новое окно как первое сообщение.

---

Привет. Продолжаем работу по проекту **I am Agent** (CRM аренды недвижимости — Expo + React Native Web + Supabase). Я владелец, не профессиональный программист, общаемся только на простом человеческом языке.

Перед тем как что-либо делать — прочитай:

1. `/Users/korshun/.claude/projects/-Users-korshun-Desktop-claude-Project-I-am-Agent-3-1/memory/MEMORY.md` и все файлы по ссылкам в нём — это правила работы со мной.
2. **Особо внимательно**: `memory/project_simple_perms_overhaul.md` — главный активный план, отражает где остановились вчера.
3. `docs/История работы/2026-04-30/00_Краткий_лог.md` — что было в прошлой сессии и какие коммиты сделаны.
4. `CLAUDE.md` и `CURSOR_RULES.md` — главные документы проекта.
5. `docs/PROGRESS_PLAN.md` — текущая доска статусов.

## Где мы остановились

**Этап 2** упрощения прав агента **закрыт** на dev/sandbox 2026-04-30. Все 13 коммитов запушены, RLS-миграция `20260429000001_simplify_properties_rls_phase2.sql` накатана в sandbox через Supabase SQL Editor владельцем. Live-тест прошёл — админ и агент могут создавать/редактировать/удалять объекты и брони.

## Что осталось из этапа 2 (документация — необязательно срочно)

**Не критично для работы CRM, можно делать постепенно:**
- `CURSOR_RULES.md` — разделы 4.1, 4.2, 4.3 переписать на две галочки `can_manage_property` / `can_manage_bookings`. Раздел 7 — пометить TD-005/009/052/055/056/057/058/059/087/111/112 + P1-001/002 как снятые.
- `docs/RULES_HUMAN/02_Компания_и_команда.html` — секция 2.5 «Права агента».
- `docs/RULES_HUMAN/03_Объекты_недвижимости.html` — секции про модерацию (3.2, 3.3, 3.4, 3.5, 3.9).
- `docs/RULES_HUMAN/04_Бронирования.html` — секции 4.1, 4.3, 4.4 (`can_book` → `can_manage_bookings`).
- `docs/RULES_HUMAN/07_Уведомления.html` — секция 7.2 (типы), 7.3 (TD), TD-таблица.
- Бонусные файлы Устава компании: `docs/CROSS_CUTTING_RULES/rbac.md`, `docs/SYSTEM_ARCHITECTURE.md`, `docs/APP_MAP_WEB.md`, `docs/TESTING_MAP.md`, файлы в папке `docs/Устав компании/`, `docs/MODULE_RULES/contacts.md`, `docs/MODULE_RULES/auth.md`, `docs/RULES_HUMAN/05_Контакты.html` — точечные упоминания старых permissions/статусов/`property_drafts`.

## Хвосты не из этапа 2 (баги нашлись при live-тесте 2026-04-30)

1. **Валюта объекта при создании:** в настройках админа стоит USD, новый объект сохраняется с THB. Отдельный TD на потом.
2. **Список объектов на странице броней не обновляется** после удаления объекта на странице «Объекты» (нужен ручной refresh). При попытке создать бронь на удалённый объект — `BOOKING_NO_COMPANY` (это правильное поведение FK). Старый UX-баг, не из этого этапа.

## Что впереди — этап 3 (cleanup)

После того как этап 2 отлежится в проде, делать **cleanup-миграцию**:
- DROP таблицы `property_drafts`, `property_rejection_history`.
- DROP колонок `properties.property_status`, `properties.rejection_reason`.
- `jsonb_strip` старых ключей прав (`can_add_property`, `can_edit_info`, `can_edit_prices`, `can_book`, `can_delete_booking`) из `company_members.permissions`.
- Упростить триггер `auto_set_property_company` — убрать упоминание `submitted`.

---

## СТРОЖАЙШИЕ ПРАВИЛА — НАРУШЕНИЕ НЕДОПУСТИМО

— Любое объяснение и обсуждение — только на человеческом языке. Технический термин → сразу перевод в скобках простыми словами.

— Сначала план словами, ждём моё «ок», только после этого код, коммит, миграция, push, правка файла. **Без явного «да» от меня — никаких действий.**

— Один шаг за сообщение. Не вываливать пять шагов сразу.

— Перед любой правкой — проверять факты в коде (имена файлов, колонки, функции). Не выдумывать.

— После каждой правки — sanity check (быстрая проверка) на совместимость с другими модулями этой сессии.

— Перед коммитом — привлекать спецагентов (Plan + tester) для проверки совместимости. По правилу `feedback_test_all_user_flows.md`.

— На live-тестирование на сайте я смотрю сам. Ты пишешь что именно надо проверить, я открываю и проверяю.

— Текущая ветка: `dev`. Push только по моей явной команде. Прод не трогаем.

— Деплой устроен так: `git push origin dev` → Vercel автоматически передеплоит `crm.iamagent.app` (1-2 мин). Sandbox-база Supabase подключена к этому домену.

— Если нужно накатить миграцию в Supabase — выведи мне SQL прямо в чат в код-блоке, я скопирую и вставлю в SQL Editor. Сам ты доступа к Supabase не имеешь.

— Я не программист. Если я что-то не понимаю — переписывай простыми словами. Не списками, не жирным шрифтом без надобности.

Работаем спокойно, пошагово, с проверками перед и после.
