# 📋 Краткий лог — 2026-04-30

## Фокус дня
Закрыт **этап 2** упрощения модели прав агента (simple-perms overhaul). Полностью выпилена модерация объектов: больше нет черновиков, статусов pending/rejected, кнопок одобрить/отклонить, уведомлений модерации. У агента теперь две галочки прав вместо пяти.

Все изменения накатаны на dev/sandbox через `crm.iamagent.app` (Vercel preview), live-тест админом и агентом прошёл по плану.

## Что сделано (13 коммитов в порядке выполнения)

| Коммит | Подкусок | Описание |
|---|---|---|
| `bcf3b34` | 2.1 | Карточка прав агента в `WebTeamSection` — два переключателя `can_manage_property` / `can_manage_bookings`. `companyService.updateMemberPermissions` пишет белым списком. i18n обновлён на en/th/ru. |
| `1315a52` | 2.2 | 17 чтений старых ключей в 7 экранах (`WebPropertiesScreen`, `WebPropertyEditPanel`, `WebBookingsScreen`, `WebBookingEditPanel`, `RealEstateScreen`, `BookingCalendarScreen`, `PropertyDetailScreen`) переведены на новые ключи. |
| `9664bfb` | 2.3.a-1 | Снят фильтр `property_status='approved'` в `AppDataContext` и мобильном `BookingCalendarScreen`. |
| `cb60bfe` | 2.3.a-2 | Мобильный список объектов `RealEstateScreen` — убрана секция «Черновики» с плашками, фильтр «На проверке», отправка `property_submitted`. `FilterBottomSheet` — убрана галочка inReview. Минус 212 строк. |
| `48e9574` | 2.3.a-3 | Мобильная карточка объекта `PropertyDetailScreen` — выпилены `handleAdminApprove`/`handleAdminReject`, ветки draft в `handleWizardSave`, displayStatus, pendingDraft, adminAgentDraft, plashки и весь блок «История отклонений», realtime-подписка на `property_drafts`. Минус 706 строк. |
| `3449b85` | 2.3.a-4 | Веб-список объектов `WebPropertiesScreen` — выпилены три ветки displayStatus в JSX, баннер draftBanner, история отклонений, вкладка «На проверке» в фильтре, модал формы причины отказа, `handleSubmitReject`/`openRejectModal`/`closeRejectModal`, draftRefreshKey/historyRefreshKey, `pendingChip` в карточке. Минус 343 строки. |
| `357e78f` | 2.3.a-5 | Веб-форма редактирования `WebPropertyEditPanel` — выпилены пропсы `reviewMode`/`onApprove`/`onReject`, ветка `submitPropertyDraft`, split direct/draft по PRICE_FIELDS, ветка `wasRejected → approveProperty`, `showSubmitLabel`. При readOnly кнопка «Сохранить» теперь не рендерится. Минус 213 строк. |
| `bd68b04` | 2.3.a-6 | Снят счётчик «На проверке» в дашборде `WebDashboardScreen` — удалены `effectivePropertyStatus`, `isApprovedEffective`, `isPendingOrRejected`, `countPendingReview`, `pendingReview` из stats и agentStats, JSX-блок `pendingReviewLine`. Минус 60 строк. |
| `0a6371e` | 2.3.b | Веб-колокольчик `WebNotificationBell` — выпилены `DiffModal`, `ACTION_TYPES`, `NAVIGABLE_TYPES`, `REJECTED_TYPES`, `handleApprove`/`handleReject`/`handleViewReview`/`handleViewEditReview`/`handleViewDiff`, `actionError` баннер, render-блоки `<DiffModal />` и `<WebPropertyEditPanel reviewMode={true} />`, мёртвые pill-кнопки и блок sd-стилей. Файл 1246 → 353 строки. |
| `e7cf7b3` | 2.3.c | `propertiesService` — выпилены `submitPropertyDraft`/`getPropertyDraft`/`getPendingDraftsForAdmin`/`approveProperty`/`rejectProperty`/`approvePropertyDraft`/`rejectPropertyDraft`/`getPropertyRejectionHistory`/`resolveCreatePropertyStatus`. В `createProperty`/`createPropertyFull` убрано `property_status`. В `deleteProperty` убраны проверка «agent + approved → throw» и чистка `notifications` модерации. `notificationsService.getPendingActionsCount` удалён. Файл 671 → 366 строк. |
| `d81efa3` | 2.3.d | Уведомления админу: `property_created` (агент создал объект) и `booking_created` (агент создал бронь). Helpers `resolveCompanyOwnerId` + `notifyAdminPropertyCreated` в `propertiesService`. Защита от self-notification. |
| `63950ce` | 2.4 | RLS фаза 2: `supabase/migrations/20260429000001_simplify_properties_rls_phase2.sql` — отключены 5 RLS-политик `property_drafts`, ALTER PUBLICATION DROP. properties INSERT/UPDATE/DELETE с проверкой `can_manage_property` + привязкой по `cm.company_id`. Admin UPDATE `properties: owner full update company` (заменил модерационную). bookings INSERT/UPDATE/DELETE — `can_manage_bookings` для агента. |
| `1c900a1` | 2.5.a | `docs/PROGRESS_PLAN.md` — закрыты 15 TD/P1 как «снято в пользу простоты»: TD-005, TD-009, TD-052, TD-055, TD-056, TD-057, TD-058, TD-059, TD-087, TD-111, TD-112, P1-001, P1-002, P2-003. Очищены упоминания в фазах 5/8/9/10. |
| `c5ba469` | 2.5.b | `docs/MODULE_RULES/notifications.md` — NT-2 переписана с новыми типами, NT-4 без `getPendingActionsCount`, TD-111/112 → снято. `company.md` — CO-PERM-1 на две галочки. `bookings.md` — `can_book`/`can_delete_booking` → `can_manage_bookings`, TD-087 → снято. `properties.md` — скоуп без модерации, таблица подмодулей 8 вместо 9, секция «Связь с RBAC» переписана, TD-таблица очищена. |

## Ручные действия
- Накатил `20260429000001_simplify_properties_rls_phase2.sql` через Supabase SQL Editor — Success (без блокеров).
- `git push origin dev` после первых 12 коммитов → Vercel передеплоил `crm.iamagent.app` (~1-2 мин).
- Live-тест: админ создал/отредактировал/удалил объект и бронь — ОК. Агент создал/отредактировал/удалил объект и бронь — ОК.

## Хвосты (не из этого этапа, всплыли при тестировании)
- Валюта объекта при создании: админ в настройках выбрал USD, новый объект сохраняется с THB. Не связано с правами.
- Список объектов на странице броней не обновляется после удаления объекта (нужен ручной refresh страницы). При попытке создать бронь на удалённый объект — `BOOKING_NO_COMPANY` (это правильное поведение FK).

## Что осталось из этапа 2 (документация)
- `CURSOR_RULES.md` — разделы 4.1/4.2/4.3 + TD-таблица в разделе 7.
- 4 HTML-файла в `docs/RULES_HUMAN/` (02, 03, 04, 07).
- Бонусные файлы Устава: `docs/CROSS_CUTTING_RULES/rbac.md`, `docs/SYSTEM_ARCHITECTURE.md`, `docs/APP_MAP_WEB.md`, `docs/TESTING_MAP.md`, файлы `docs/Устав компании/*`, `docs/MODULE_RULES/contacts.md`, `docs/MODULE_RULES/auth.md`, `docs/RULES_HUMAN/05_Контакты.html` — точечные правки.

## Что впереди — этап 3 (cleanup)
DROP таблиц `property_drafts`, `property_rejection_history`. DROP колонок `properties.property_status`, `properties.rejection_reason`. `jsonb_strip` старых ключей прав из `company_members.permissions`. Упрощение триггера `auto_set_property_company`. Делать после полного релиза этапа 2 с отлёжкой.

## Финальные коммиты дня
- `c5ba469` — `docs(perms): этап 2.5.b — правила модулей под упрощённую модель прав`
- (далее) — обновление памяти + лог дня (этот файл) + стартовый промт для следующей сессии.

## Уроки сессии
- Этап 2 формально разбит на 14 подкусков (1, 2, 3a×6, 3b, 3c, 3d, 4, 5a, 5b) — это длинно, но позволяет каждый коммит легко откатить отдельно. Tester нашёл два блокера в RLS-миграции (отсутствие `cm.company_id`), оба исправлены до коммита.
- Колокольчик уведомлений сначала собирался полностью удалить — отговорил владелец («колокольчик это телевизор, что отправляют то и показывает»). Правильно — выпилили только мёртвые кнопки и подписку на `property_drafts`, сам колокольчик и обычные уведомления остались.
- Live-тестирование на `crm.iamagent.app` владелец делает сам — я только готовлю и говорю что нажать.
