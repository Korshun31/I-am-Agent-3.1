# 📝 Краткий лог — 2026-03-28

> Заполнять каждый час или при завершении значимого этапа.  
> Обязательно: Задача + Действие + Результат + Следующий шаг.

---

| Время | Задача | Действие | Результат | Следующий шаг | Риск / Блокер |
|-------|--------|----------|-----------|---------------|---------------|
| ~09:00 | Hotfix: `properties.updated_at` не найден | Удалил `updated_at` из `.update()` в `rejectProperty` и `rejectPropertyDraft` (`propertiesService.js`) | Ошибка схемы исчезла; reject работает без alert | Проверить прогон 5 отклонений подряд | — |
| ~09:30 | Refresh истории без `updated_at` | Ввёл `historyRefreshKey` state в `WebPropertiesScreen`, прокинул в `PropertyDetail`, добавил в deps useEffect | История обновляется после каждого reject | Проверить bell-flow | — |
| ~10:00 | Hotfix: история не обновляется после reject из Bell | В `useEffect([refreshKey])` добавил `setHistoryRefreshKey(k => k + 1)` | История обновляется из любого источника reject | — | — |
| ~11:00 | Документация: `docs/Устав компании/` | Создал 12 файлов (оглавление, ADR, SQL-реестр, QA, RLS, бизнес-сценарии, инциденты, sanity-check) | База знаний проекта структурирована | Обновить APP_MAP_WEB.md и SYSTEM_ARCHITECTURE.md | — |
| ~11:30 | Обновление существующих docs | Добавил секции review-flow, статусы, история отклонений в `APP_MAP_WEB.md` и `SYSTEM_ARCHITECTURE.md` | Оба документа актуальны | — | — |
| ~12:00 | Система журналирования `docs/История работы/` | Создал README, шаблоны (5 файлов) и папку текущего дня с заполненными стартовыми данными | Журнальная система готова к использованию | — | — |

---

## Итог дня

**Что сделано:**  
Hotfix reject-flow (ошибка `updated_at`), ввод `historyRefreshKey` для надёжного refresh истории, полная документация проекта (Устав компании — 12 файлов, обновление APP_MAP_WEB + SYSTEM_ARCHITECTURE), система журналирования История работы.

**Что не сделано (и почему):**  
Backfill legacy rejection_reason → требует staging-тестирования, отложено.  
`can_moderate_properties` → proposal, не реализован.

**Главный вывод дня:**  
Проект получил структурированную базу знаний; hotfix `updated_at` устранил критическую блокировку reject-flow.
